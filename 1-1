#include<stdio.h>
int main(){
	int i , j , z;
	for (i=1;i<5;i++){
		for (j=1;j<5;j++){
    if (i==j)
    continue;
			for (z=1;z<5;z++){
      if (i==z || j==z)
      continue;
				if (i!=j && i!=z && j!=z )
				printf("%d%d%d\n",i,j,z);
	}}}
	return ;
}
